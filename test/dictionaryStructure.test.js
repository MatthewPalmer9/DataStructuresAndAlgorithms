const Dictionary = require('../dictionaryStructure.js');

describe('Dictionary', () => {

    let dict;
    beforeEach(() => {
        dict = new Dictionary();
    });

    it('should successfully addman element to the dictionary', () => {
        
    });
});